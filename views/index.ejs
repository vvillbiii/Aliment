<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Bootstrap CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <!--- CSS -->
    <link rel="stylesheet" href="/main.css" />
    <title>All Restaurants in New York City</title>
  </head>
  <body>
    <!-- Navbar section -->
    <%- include('../views/partials/navbar') %>
    <div class="container">
      <div class="miniheader">
        <h1 class="text-center">All Restaurants in New York City</h1>
      </div>

      <% for(let r of Restaurant){ %>
      <div class="card">
        <img src="<%= r.image %>" class="card-img" alt="<%= r.name %>" />
        <div class="card-body">
          <h3 class="card-title"><%= r.name %></h3>
          <div>
            <p>
              <%= r.address.street %>, <%= r.address.city %>, <%=
              r.address.state %>
            </p>
          </div>
          <p><%= r.price %> $</p>
          <a href="/restaurants/<%= r._id %>"
            ><button class="btn btn-primary">View Restaurant Details</button></a
          >
        </div>
      </div>
      <% }%>
    </div>
  </body>
</html>
